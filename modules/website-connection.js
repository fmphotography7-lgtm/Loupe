window.SFWebsiteConnection={
 ensure(){const s=window.SF.state;s.websiteAnalytics=s.websiteAnalytics||{period:'Last 30 days',visitors:0,newUsers:0,visits:0,pageViews:0,bounceRate:0,engagementRate:0,avgDuration:0,orders:0,revenue:0,sources:[],pages:[],countries:[],funnel:{galleryViews:0,productViews:0,cartAdds:0,checkoutStarts:0},updatedAt:''};s.googleAnalytics=s.googleAnalytics||{status:'Not Connected',propertyName:'',propertyId:'',lastSync:'',source:'Live GA4',syncFrequency:'Daily'}},
 async render(){this.ensure();const sf=window.SF,C=window.SFCommerceHub,sq=sf.state.squarespace||{},site=sq.website||{},a=sf.state.websiteAnalytics,g=sf.state.googleAnalytics;const status=await sf.api.gaStatus?.()||{};
 sf.$('workspace').innerHTML=`<div class="page-stack website-connection-page"><section class="dashboard-hero"><div><div class="section-kicker">WEBSITE · CONNECTION MANAGER</div><h2>Website Connections</h2><p>Connect once, then let StudioFlow sync the business signals behind the scenes.</p></div><i class="stock-pill ${C.connectionClass()}">${sf.esc(sq.connectionStatus||'Not Connected')}</i></section>
 <section class="card"><div class="commerce-toolbar"><div><div class="section-kicker">SQUARESPACE</div><h3>Store Connection · ${sq.connectionStatus==='Connected'?'✓ Connected':'Disconnected'}</h3></div><i class="stock-pill ${C.connectionClass()}">${sf.esc(sq.connectionStatus||'Not Connected')}</i></div><div class="connection-grid"><label>Squarespace API Key<input id="wcApiKey" type="password" placeholder="Paste API key to connect or replace saved key"></label><label>Automatically check for orders<select id="wcAuto"><option value="yes" ${sq.autoSync!==false?'selected':''}>Enabled</option><option value="no" ${sq.autoSync===false?'selected':''}>Off</option></select></label><label>Check every<select id="wcMinutes"><option value="15" ${Number(sq.syncMinutes||15)===15?'selected':''}>15 minutes</option><option value="30" ${Number(sq.syncMinutes)===30?'selected':''}>30 minutes</option><option value="60" ${Number(sq.syncMinutes)===60?'selected':''}>1 hour</option><option value="0" ${Number(sq.syncMinutes)===0?'selected':''}>Manual only</option></select></label></div><div class="row-actions"><button class="button secondary" id="wcSave">Save Connection</button><button class="button secondary" id="wcTest">Test Connection</button><button class="button secondary" id="wcProducts">Sync Products</button><button class="button secondary" id="wcInspect">Inspect Raw Product Data</button><button class="button secondary" id="wcArtCardCheck">Check Art Card Coverage</button><button class="button primary" id="wcOrders">Check for Orders</button>${sq.connectionStatus==='Connected'?'<button class="button danger" id="wcDisconnect">Disconnect</button>':''}</div><div class="connection-meta">${site.title?`<b>${sf.esc(site.title)}</b> · ${sf.esc(site.url||'')} · ${sf.esc(site.currency||'')}`:'No website details loaded yet.'}${sq.lastOrderSync?`<br>Last successful order check: ${new Date(sq.lastOrderSync).toLocaleString()}`:'<br>Last successful order check: Never'}${sq.lastProductSync?`<br>Last successful product sync: ${new Date(sq.lastProductSync).toLocaleString()}`:''}${sq.lastError?`<br><span class="danger-text">${sf.esc(sq.lastError)}</span>`:''}</div></section>
 <section class="card"><div class="commerce-toolbar"><div><div class="section-kicker">DEVELOPER TOOLS</div><h3>Simulate a Website Order</h3><p class="muted">Creates a realistic test order without touching your live Squarespace store.</p></div></div><div class="row-actions"><button class="button secondary" data-sim-order="Art Card">Art Card</button><button class="button secondary" data-sim-order="Framed Print">Framed Print</button><button class="button secondary" data-sim-order="Canvas">Canvas</button><button class="button secondary" data-sim-order="Multiple Items">Multiple Items</button></div></section>
 <section class="card"><div class="commerce-toolbar"><div><div class="section-kicker">DEVELOPER TOOLS</div><h3>System Information</h3><p class="muted">What's actually loaded, for debugging.</p></div></div><div class="commerce-table"><div class="commerce-row"><span>Application Version</span><span></span><span><b>${sf.esc(sf.state.appVersion||'—')}</b></span></div><div class="commerce-row"><span>Squarespace Service</span><span></span><span><i class="stock-pill ${window.SquarespaceService?'success':'danger'}">${window.SquarespaceService?'Loaded':'Not Loaded'}</i></span></div><div class="commerce-row"><span>Inventory Service</span><span></span><span><i class="stock-pill ${window.InventoryService?'success':'danger'}">${window.InventoryService?'Loaded':'Not Loaded'}</i></span></div><div class="commerce-row"><span>Materials Service</span><span></span><span><i class="stock-pill ${window.MaterialsService?'success':'danger'}">${window.MaterialsService?'Loaded':'Not Loaded'}</i></span></div><div class="commerce-row"><span>Product Service</span><span></span><span><i class="stock-pill ${window.ProductService?'success':'danger'}">${window.ProductService?'Loaded (Frame/Mat data excluded -- Room Designer protected)':'Not Loaded'}</i></span></div><div class="commerce-row"><span>Production Service / Recipe Engine</span><span></span><span><i class="stock-pill gold">Loaded (interfaces only -- Recipe Engine: Not Installed)</i></span></div><div class="commerce-row"><span>Analytics Service</span><span></span><span><i class="stock-pill ${window.AnalyticsService?'success':'danger'}">${window.AnalyticsService?'Loaded (core revenue calculations)':'Not Loaded'}</i></span></div><div class="commerce-row"><span>Ink Cost Engine</span><span></span><span><i class="stock-pill success">Loaded (Phase D1-D5 complete -- D5 direct printer connection is experimental and untested against real hardware)</i></span></div><div class="commerce-row"><span>Squarespace Connection</span><span></span><span><i class="stock-pill ${C.connectionClass()}">${sf.esc(sq.connectionStatus||'Not Connected')}</i></span></div></div></section>
 <section class="card ga-setup-card"><div class="commerce-toolbar"><div><div class="section-kicker">GOOGLE ANALYTICS 4</div><h3>Google Analytics Connection</h3><p class="muted">A guided, read-only connection. StudioFlow never stores your Google password.</p></div><i class="stock-pill ${status.connected?'connected':'not-connected'}">${status.connected?'Connected':status.credentials?'Ready to sign in':'Setup required'}</i></div>
 <div class="ga-steps"><div class="ga-step ${status.credentials?'done':'active'}"><b>1</b><span><strong>Google Cloud credentials</strong><small>${status.credentials?`Loaded${status.projectId?` · ${sf.esc(status.projectId)}`:''}`:'Create a Desktop OAuth client, then import its JSON file.'}</small></span><div><button class="button secondary" id="gaOpenCloud">Open Google Cloud</button><button class="button ${status.credentials?'secondary':'primary'}" id="gaCredentials">${status.credentials?'Replace JSON':'Import OAuth JSON'}</button></div></div>
 <div class="ga-step ${status.connected?'done':status.credentials?'active':''}"><b>2</b><span><strong>Connect Google account</strong><small>${status.connected?'Google authorization is securely saved.':'Sign in with the account that owns Frozen Moments Photography.'}</small></span><button class="button ${status.credentials&&!status.connected?'primary':'secondary'}" id="gaConnect" ${status.credentials?'':'disabled'}>${status.connected?'Reconnect':'Connect Google Account'}</button></div>
 <div class="ga-step ${g.propertyId?'done':status.connected?'active':''}"><b>3</b><span><strong>Select Analytics property</strong><small>${g.propertyId?`${sf.esc(g.propertyName||'Selected property')} · ${sf.esc(g.propertyId)}`:'StudioFlow will list the GA4 properties available to your account.'}</small></span><div><select id="gaProperty"><option value="${sf.esc(g.propertyId||'')}">${g.propertyId?sf.esc(g.propertyName||g.propertyId):'Connect first'}</option></select><button class="button secondary" id="gaLoadProperties" ${status.connected?'':'disabled'}>Load Properties</button></div></div>
 <div class="ga-step ${g.lastSync?'done':g.propertyId?'active':''}"><b>4</b><span><strong>Sync analytics</strong><small>${g.lastSync?`Last sync: ${new Date(g.lastSync).toLocaleString()}`:'Pull visitors, traffic sources, top pages and countries.'}</small></span><div><select id="gaFrequency"><option ${g.syncFrequency==='Manual'?'selected':''}>Manual</option><option ${g.syncFrequency==='Daily'?'selected':''}>Daily</option><option ${g.syncFrequency==='Hourly'?'selected':''}>Hourly</option></select><button class="button primary" id="gaSync" ${g.propertyId?'':'disabled'}>Sync Now</button></div></div></div>
 <details class="manual-tools"><summary>Manual analytics tools</summary><div class="row-actions"><input id="gaCsv" type="file" accept=".csv" hidden><button class="button secondary" id="gaImport">Import GA4 CSV</button><button class="button secondary" id="gaOpen">Open Google Analytics</button><button class="button danger" id="gaDisconnect">Disconnect Google</button></div></details></section>
 <section class="card"><div class="commerce-toolbar"><div><div class="section-kicker">LIVE WEBSITE INSIGHTS</div><h3>Latest Analytics</h3></div><span class="muted">${a.updatedAt?new Date(a.updatedAt).toLocaleString():'Waiting for first sync'}</span></div><div class="commerce-kpis"><div><b>${Number(a.visitors||0).toLocaleString()}</b><span>Active Users</span></div><div><b>${Number(a.newUsers||0).toLocaleString()}</b><span>New Users</span></div><div><b>${Number(a.pageViews||0).toLocaleString()}</b><span>Page Views</span></div><div><b>${Math.round(Number(a.avgDuration||0))}s</b><span>Avg. Engagement</span></div></div><div class="notice"><b>Read-only safety:</b> Google Analytics data can inform StudioFlow, but StudioFlow cannot alter your website or Analytics property through this connection.</div></section></div>`;
 sf.$('wcSave').onclick=async()=>{const key=sf.$('wcApiKey').value.trim();sq.autoSync=sf.$('wcAuto').value==='yes';sq.syncMinutes=Number(sf.$('wcMinutes').value??15);if(key){const r=await sf.api.squarespaceSaveCredentials?.({apiKey:key});if(!r?.ok)return alert(r?.error||'Could not save key.')}await sf.persist();C.startAutoSync();alert('Squarespace connection settings saved.');this.render()};
 sf.$('wcTest').onclick=async()=>{const key=sf.$('wcApiKey').value.trim();const r=await sf.api.squarespaceTest?.({apiKey:key});if(r?.ok){sq.connectionStatus='Connected';sq.website=r.website||{};sq.lastError=''}else{sq.connectionStatus='Connection Error';sq.lastError=r?.error||'Connection failed'}await sf.persist();this.render()};sf.$('wcProducts').onclick=()=>C.syncProducts();sf.$('wcOrders').onclick=()=>C.syncOrders(false);
 sf.$('wcInspect').onclick=()=>{
   const products=sf.state.websiteProducts||[];
   if(!products.length)return alert('No synced product data yet -- run Sync Products first.');
   // Show one product WITH variant options that parsed correctly (if any) and a couple of others,
   // so the actual raw shape of the data StudioFlow is working with is visible instead of guessed
   // at -- this is read-only, just for diagnosing why variant matching finds so few products.
   const withOptions=products.find(p=>(p.variants||[]).some(v=>(v.variantOptions||v.options||[]).length));
   const sample=[withOptions,...products.filter(p=>p!==withOptions)].filter(Boolean).slice(0,3);
   sf.$('modalRoot').innerHTML=`<div class="modal-backdrop"><div class="modal-card wide"><h2>Raw Product Data Sample</h2><p class="muted">First 3 products from your last sync, shown as-is. Screenshot or copy this to share for troubleshooting.</p><pre style="max-height:60vh;overflow:auto;background:#0b1117;padding:14px;border-radius:8px;font-size:11px;color:#dfe7ee">${sf.esc(JSON.stringify(sample,null,2))}</pre><div class="row-actions"><button class="button primary" id="wcInspectClose">Close</button></div></div></div>`;
   sf.$('wcInspectClose').onclick=()=>sf.closeModal();
 };
 sf.$('wcArtCardCheck').onclick=()=>{
   const variants=C?.liveVariants?C.liveVariants():[];
   if(!variants.length)return alert('No synced product data yet -- run Sync Products first.');
   sf.state.acknowledgedNoArtCard=Array.isArray(sf.state.acknowledgedNoArtCard)?sf.state.acknowledgedNoArtCard:[];
   const byProduct=new Map();
   variants.forEach(v=>{
     if(!byProduct.has(v.productId))byProduct.set(v.productId,{title:v.title,hasArtCard:false});
     if(/art card/i.test(v.variant))byProduct.get(v.productId).hasArtCard=true;
   });
   const missing=[...byProduct.entries()].filter(([id,p])=>!p.hasArtCard&&!sf.state.acknowledgedNoArtCard.includes(id));
   const acknowledged=[...byProduct.entries()].filter(([id])=>sf.state.acknowledgedNoArtCard.includes(id));
   const row=([id,p])=>`<div class="commerce-row"><span><b>${sf.esc(p.title)}</b></span><span></span><span class="row-actions"><button class="button primary" data-add-artcard="${sf.esc(id)}" data-title="${sf.esc(p.title)}">Add Art Card Variant</button><button class="button secondary" data-ack-no-artcard="${sf.esc(id)}">Mark as Intentional</button></span></div>`;
   sf.$('modalRoot').innerHTML=`<div class="modal-backdrop"><div class="modal-card wide"><h2>Art Card Coverage</h2><p class="muted">${byProduct.size} products checked. ${missing.length} have no Art Card variant.</p><div class="commerce-table">${missing.map(row).join('')||'<div class="empty-state">Every product (other than ones you\'ve marked intentional) has an Art Card variant.</div>'}</div>${acknowledged.length?`<h3>Marked Intentional (${acknowledged.length})</h3><div class="commerce-table">${acknowledged.map(([id,p])=>`<div class="commerce-row"><span>${sf.esc(p.title)}</span><span></span><span><button class="button secondary" data-unack-no-artcard="${sf.esc(id)}">Unmark</button></span></div>`).join('')}</div>`:''}<div class="row-actions"><button class="button primary" id="wcArtCardClose">Close</button></div></div></div>`;
   sf.$('wcArtCardClose').onclick=()=>sf.closeModal();
   document.querySelectorAll('[data-ack-no-artcard]').forEach(b=>b.onclick=async()=>{sf.state.acknowledgedNoArtCard.push(b.dataset.ackNoArtcard);await sf.persist();sf.$('wcArtCardCheck').click()});
   document.querySelectorAll('[data-unack-no-artcard]').forEach(b=>b.onclick=async()=>{sf.state.acknowledgedNoArtCard=sf.state.acknowledgedNoArtCard.filter(x=>x!==b.dataset.unackNoArtcard);await sf.persist();sf.$('wcArtCardCheck').click()});
   document.querySelectorAll('[data-add-artcard]').forEach(b=>b.onclick=()=>this.addArtCardVariant(b.dataset.addArtcard,b.dataset.title));
 };
 const wcDisc=sf.$('wcDisconnect');if(wcDisc)wcDisc.onclick=async()=>{if(!confirm('Disconnect from Squarespace? Automatic order checking will stop until you reconnect.'))return;sq.connectionStatus='Not Connected';clearInterval(C._sqTimer);await sf.persist();this.render()};
 document.querySelectorAll('[data-sim-order]').forEach(b=>b.onclick=async()=>{window.SFWebsiteUpdates.simulateOrder(b.dataset.simOrder);await sf.persist();C.refreshOrderAttention?.();alert(`Simulated ${b.dataset.simOrder} order created. Open Sales & Orders to see it.`)});
 sf.$('gaOpenCloud').onclick=()=>sf.api.openExternal?.('https://console.cloud.google.com/apis/credentials');
 sf.$('gaCredentials').onclick=async()=>{const r=await sf.api.gaImportCredentials?.();if(r?.ok){alert('Google OAuth credentials imported securely.');this.render()}else if(r)alert(r.error||'Could not import credentials.')};
 sf.$('gaConnect').onclick=async()=>{const b=sf.$('gaConnect');b.disabled=true;b.textContent='Waiting for Google…';const r=await sf.api.gaConnect?.();if(r?.ok){g.status='Connected';await sf.persist();alert('Google Analytics connected. Now load and select your property.');this.render()}else{b.disabled=false;b.textContent='Connect Google Account';alert(r?.error||'Google sign-in did not complete.')}};
 sf.$('gaLoadProperties').onclick=async()=>{const r=await sf.api.gaListProperties?.();if(!r?.ok)return alert((r?.error||'Could not load properties.')+' Make sure both Google Analytics Data API and Google Analytics Admin API are enabled.');const sel=sf.$('gaProperty');sel.innerHTML='<option value="">Select a property</option>'+r.properties.map(x=>`<option value="${sf.esc(x.id)}" data-name="${sf.esc(x.name)}">${sf.esc(x.name)}${x.account?` · ${sf.esc(x.account)}`:''}</option>`).join('');if(g.propertyId)sel.value=g.propertyId;sel.onchange=async()=>{g.propertyId=sel.value;g.propertyName=sel.selectedOptions[0]?.dataset.name||sel.selectedOptions[0]?.textContent||'';await sf.persist();this.render()}};
 sf.$('gaFrequency').onchange=async e=>{g.syncFrequency=e.target.value;await sf.persist()};
 sf.$('gaSync').onclick=async()=>{const b=sf.$('gaSync');b.disabled=true;b.textContent='Syncing…';const r=await sf.api.gaSync?.({propertyId:g.propertyId});if(!r?.ok){b.disabled=false;b.textContent='Sync Now';return alert(r?.error||'Analytics sync failed.')}Object.assign(a,r.analytics||{});a.updatedAt=r.syncedAt;g.lastSync=r.syncedAt;g.status='Connected';g.source='Live GA4';sf.logActivity('Synced live Google Analytics data');await sf.persist();this.render();alert('Google Analytics synced. The Home Dashboard has been updated.')};
 sf.$('gaOpen').onclick=()=>sf.api.openExternal?.('https://analytics.google.com/analytics/web/');sf.$('gaImport').onclick=()=>sf.$('gaCsv').click();sf.$('gaCsv').onchange=e=>this.importCsv(e.target.files?.[0],true);sf.$('gaDisconnect').onclick=async()=>{if(!confirm('Disconnect Google Analytics from StudioFlow?'))return;await sf.api.gaDisconnect?.();g.status='Not Connected';g.propertyId='';g.propertyName='';g.lastSync='';await sf.persist();this.render()}
 },
 addArtCardVariant(productId,title){
   const sf=window.SF;
   sf.$('modalRoot').innerHTML=`<div class="modal-backdrop"><div class="modal-card"><h2>Add Art Card Variant</h2><p class="muted">${sf.esc(title)}</p><label>Size (e.g. 5 x 7)<input id="acSize" value="5 x 7"></label><label>Price<input id="acPrice" type="number" min="0" step=".01" value="8.00"></label><label>SKU (leave blank to auto-generate in your existing style)<input id="acSku" placeholder="e.g. SQ1234567"></label><div class="row-actions"><button class="button secondary" id="acCancel">Cancel</button><button class="button primary" id="acSave">Add to Squarespace</button></div></div></div>`;
   sf.$('acCancel').onclick=()=>sf.closeModal();
   sf.$('acSave').onclick=async()=>{
     const size=sf.$('acSize').value.trim(), price=Number(sf.$('acPrice').value)||0;
     // Squarespace's API requires a SKU on creation, even though the normal product editor
     // auto-generates one for you and never makes you think about it. Match your existing
     // pattern (SQ + 7 digits) when left blank instead of sending nothing.
     const sku=sf.$('acSku').value.trim()||`SQ${Math.floor(1000000+Math.random()*9000000)}`;
     if(!size||price<=0)return alert('Enter a size and a price greater than $0.');
     if(!confirm(`Add an Art Card variant (${size}, $${price.toFixed(2)}, SKU ${sku}) to "${title}" on your live Squarespace store now?`))return;
     const result=await sf.api.squarespaceRestoreVariant({productId,attributes:{Material:'Art Card',Size:size},sku,price,currency:sf.state.business?.currency||'CAD'});
     if(result?.ok){
       sf.logActivity(`Added Art Card variant (${size}) to ${title} via API`);
       await sf.persist();
       sf.closeModal();
       alert('Added. Run Sync Products to refresh StudioFlow\'s copy of your catalogue.');
     }else{
       alert(`Could not add the variant: ${result?.error||'Unknown error'}`);
     }
   };
 },
 async importCsv(file,isGa4=false){if(!file)return;const sf=window.SF,text=await file.text(),lines=text.split(/\r?\n/).filter(Boolean),rows=lines.map(x=>x.split(',').map(v=>v.replace(/^"|"$/g,'').trim())),norm=x=>String(x||'').toLowerCase().replace(/[^a-z0-9]/g,''),num=x=>Number(String(x||'').replace(/[$,%\s]/g,'').replace(/,/g,''))||0,a=sf.state.websiteAnalytics;for(const r of rows){const k=norm(r[0]),v=num(r[1]);if(/uniquevisitors|activeusers|visitors/.test(k))a.visitors=v;else if(/newusers/.test(k))a.newUsers=v;else if(/visits|sessions/.test(k))a.visits=v;else if(/pageviews/.test(k))a.pageViews=v;else if(/bouncerate/.test(k))a.bounceRate=v;else if(/averageduration|avgduration|averageengagementtime/.test(k))a.avgDuration=v}a.updatedAt=new Date().toISOString();a.sourceFile=file.name;if(isGa4){sf.state.googleAnalytics.status='Connected by GA4 CSV';sf.state.googleAnalytics.lastSync=a.updatedAt;sf.state.googleAnalytics.source='GA4 CSV'}sf.logActivity(`Imported website analytics from ${file.name}`);await sf.persist();this.render();alert('Website analytics imported. The Home Dashboard has been updated.')}
};
