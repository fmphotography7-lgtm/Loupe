/* ============================================================
   overrides.js  —  YOUR file. The export never touches it.
   ------------------------------------------------------------
   Rename this to  overrides.js  to switch it on, and drop any
   pictures you refer to into the  images  folder next to it.

   Everything here runs after the exported catalogue loads, so
   whatever you set below wins. Delete a line to go back to the
   exported value.
   ============================================================ */

/* A hero photograph that isn't one of your catalogue pieces */
// FM.brand.hero = "images/my-hero.jpg";

/* The portrait of you on the About section */
// FM.brand.portrait = "images/kirk.jpg";

/* A cover for one gallery — use the gallery's name */
// FM.galleries.forEach(function (g) {
//   if (g.name === "Wildlife") g.cover = "images/wildlife-cover.jpg";
// });

/* Anything else in the catalogue can be changed the same way. */
